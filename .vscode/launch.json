{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Hugo Server",
            "type": "node-terminal",
            "request": "launch",
            "command": "${workspaceFolder}/.vscode/start-hugo.sh",
            "cwd": "${workspaceFolder}"
        }
    ]
}